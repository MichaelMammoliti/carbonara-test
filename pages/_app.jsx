import React from 'react';

import '../styles/_reset.scss';
import '../translations/i18n.config';

import '../styles/slick-fixes.css';

const App = ({ Component, pageProps }) => (
  <Component {...pageProps} />
);

export default App;
